---
import { CV } from "@/data/cv";

const { about } = CV;
---

<section class="relative w-full flex flex-col md:flex-row justify-center px-6 md:px-12 lg:px-24 py-24 overflow-hidden bg-brand-black">
  <div class="text-white">
    {about.title}
  </div>
  <div class="text-white">
    {about.description}
  </div>
</section>